# E2E Test Configuration
# Copy this file to .env.tests and fill in your values

# Base URL of the running MCP Lurky server
# When running via Docker Compose, this should be http://localhost:8112
# When running locally, this should be http://localhost:8000
MCP_LURKY_TEST_BASE_URL="http://localhost:8112"

# Request timeout for E2E tests (seconds)
MCP_LURKY_TEST_TIMEOUT_SECONDS=60

# Private key for x402 payment testing (wallet must have USDC on the configured network)
# For mainnet testing: use a wallet with real USDC on Base (chain_id: 8453)
# For testnet testing: use a wallet with testnet USDC on Base Sepolia (chain_id: 84532)
MCP_LURKY_TEST_PRIVATE_KEY=

# Lurky API key for Lurky endpoint testing
MCP_LURKY_TEST_LURKY_API_KEY=
