# --- Lurky Service Specific Settings ---
# These are loaded by the Lurky service's own config.
LURKY_API_KEY="your_lurky_api_key_here"
LURKY_BASE_URL=https://api.lurky.app

# --- Database Settings (Optional - for caching) ---
# The database is used for caching Twitter Spaces summaries to reduce API calls.
# If not configured, the server will fail to start (database is currently required).
# To run without cache, you would need to modify the code to make database optional.
DB_NAME=lurky_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT_RAW=5432

# Cache TTL defaults (in seconds)
CACHE_TTL_SEARCH=3600  # 1 hour
CACHE_TTL_DETAILS=86400  # 24 hours

# -----------------------------------------------------------------------------
# x402 Protocol Settings
# -----------------------------------------------------------------------------
# Enable or disable x402 payment enforcement.
# Set to "off" to disable payments (useful for internal/free deployments).
# Set to "on" to enable payments (default).
MCP_LURKY_X402_PRICING_MODE=on

# This is the wallet address where you will receive payments.
# IMPORTANT: Replace with your own Ethereum wallet address.
MCP_LURKY_X402_PAYEE_WALLET_ADDRESS="0x..."

# (Optional) For authenticated access to the CDP Facilitator on mainnet.
# See: https://docs.cdp.coinbase.com/x402/quickstart-for-sellers#running-on-mainnet
# MCP_LURKY_X402_FACILITATOR_URL="https://x402.org/facilitator/validate"
# MCP_LURKY_X402_CDP_API_KEY_ID=""
# MCP_LURKY_X402_CDP_API_KEY_SECRET=""

# Path to the YAML file containing the pricing configuration for all tools.
# This allows for a much cleaner and more readable pricing setup than using
# nested environment variables.
MCP_LURKY_X402_PRICING_CONFIG_PATH="tool_pricing.yaml"

# -----------------------------------------------------------------------------
# MCP Lurky Server - App Settings
# -----------------------------------------------------------------------------
MCP_LURKY_HOST=0.0.0.0
MCP_LURKY_PORT=8000
MCP_LURKY_LOGGING_LEVEL=INFO
MCP_LURKY_HOT_RELOAD=false
