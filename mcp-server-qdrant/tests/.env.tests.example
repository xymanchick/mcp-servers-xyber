# E2E Test Configuration
# Copy this file to .env.tests and configure values

# URL of the running Qdrant MCP server for e2e tests
# Start the server with: uv run uvicorn mcp_server_qdrant.app:create_app --factory --port 8000
E2E_SERVER_URL=http://localhost:8000
