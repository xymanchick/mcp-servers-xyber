"""
MCP-only routers.

Kept for parity with `mcp-server-lurky`. For this server, all current functionality
is exposed via `hybrid_routers` (REST + MCP), but this package allows MCP-only tools
to be added later without changing the app wiring.
"""

routers = []

